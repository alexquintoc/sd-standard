import { updatePath, type Update } from "@/lib/updates";
export function UpdateCard({ update }: { update:Update }) { const href=updatePath(update); return <article className="overflow-hidden border-t border-[#d9d4c8]">{update.featuredImage ? <a href={href} tabIndex={-1}><img className="aspect-[3/2] w-full object-cover" src={update.featuredImage} alt={update.imageAlt ?? ""} /></a> : null}<div className="p-6"><div className="mb-3 flex flex-wrap gap-3 text-xs font-bold text-[#5f5a50]"><span className="uppercase text-[#28775e]">{update.category}</span><time dateTime={update.publishedDate}>{new Date(update.publishedDate).toLocaleDateString(update.locale, {dateStyle:"long", timeZone:"UTC"})}</time></div><h2 className="text-xl font-extrabold"><a className="hover:text-[#28775e] focus:outline-none focus:ring-4 focus:ring-[#85bba8]" href={href}>{update.title}</a></h2>{update.summary ? <p className="mt-3 text-sm leading-6 text-[#5f5a50]">{update.summary}</p> : null}</div></article>; }
