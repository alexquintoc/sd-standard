{
  "name": "sd-standard",
  "private": true,
  "scripts": {
    "build:site": "node scripts/build-site.js",
    "docs:generate": "node scripts/generate-docs.js",
    "generate-docs": "npm run docs:generate",
    "docs:serve": "npm run docs:generate && mdbook serve docs",
    "docs:build": "npm run docs:generate && mdbook build docs",
    "dev": "cross-env TINA_PUBLIC_IS_LOCAL=true PORT=5000 tinacms dev -c \"npm run dev -w apps/calculator\"",
    "dev:app": "cross-env PORT=5000 npm run dev -w apps/calculator",
    "build": "npm run validate:public && node scripts/sync-tina-admin.js && npm run build -w apps/calculator",
    "test": "npm test -w packages/standard-core && npm run test:workspace -w apps/calculator && node scripts/test-provider.mjs && node scripts/test-public.mjs && npm run test:updates -w apps/calculator && npm run test:impact-snapshot -w apps/calculator && npm run test:brief-generator -w apps/calculator",
    "check": "npm run check -w packages/standard-core && npm run check -w apps/calculator",
    "lint": "npm run lint -w apps/calculator",
    "validate:public": "node scripts/test-public.mjs"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "@tinacms/cli": "^2.3.1",
    "@tinacms/datalayer": "^2.0.21",
    "tinacms": "^3.8.1"
  },
  "devDependencies": {
    "cross-env": "^10.1.0"
  }
}
